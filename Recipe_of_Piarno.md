# Piarno_KBL_1D 技術仕様書 (Technical Specification)

本ドキュメントは、単眼RGBカメラのみを用いて机面タップ（打鍵）を高精度かつゼロレイテンシで認識し、ブラウザ上でリアルタイムピアノ演奏を実現する `Piarno_KBL_1D` の数理モデル、信号処理アルゴリズム、音響合成、システムアーキテクチャの完全な技術詳細をまとめたものです。

---

## 目次

1. [システム概要と設計思想](#1-システム概要と設計思想)
2. [単眼ビジョンにおける打鍵認識の課題](#2-単眼ビジョンにおける打鍵認識の課題)
3. [幾何・空間正規化アルゴリズム（数理モデル）](#3-幾何空間正規化アルゴリズム数理モデル)
   - [3.1 机面水平ローアングル配置](#31-机面水平ローアングル配置)
   - [3.2 手の基準長 $L$ による空間正規化](#32-手の基準長-l-による空間正規化)
   - [3.3 無次元化相対垂直変位 $r_y$ の導出](#33-無次元化相対垂直変位-r_y-の導出)
4. [信号処理パイプライン（1 Euro Filter）](#4-信号処理パイプライン1-euro-filter)
   - [4.1 採用理由とトレードオフ](#41-採用理由とトレードオフ)
   - [4.2 1 Euro Filter の数理モデル](#42-1-euro-filter-の数理モデル)
   - [4.3 実装パラメータと動的カットオフ動作](#43-実装パラメータと動的カットオフ動作)
5. [打鍵判定ステートマシンとヒステリシス制御](#5-打鍵判定ステートマシンとヒステリシス制御)
   - [5.1 状態遷移モデル（IDLE / TOUCHED）](#51-状態遷移モデルidle--touched)
   - [5.2 チャタリング遮断と高速連打（トリル）の成立](#52-チャタリング遮断と高速連打トリルの成立)
6. [データ駆動型・指別自動キャリブレーション](#6-データ駆動型指別自動キャリブレーション)
   - [6.1 生体力学的な指ごとの可動域特性](#61-生体力学的な指ごとの可動域特性)
   - [6.2 CSVデータセット構造](#62-csvデータセット構造)
   - [6.3 統計的閾値導出アルゴリズム](#63-統計的閾値導出アルゴリズム)
7. [ゼロレイテンシ音響合成エンジン（Web Audio API）](#7-ゼロレイテンシ音響合成エンジンweb-audio-api)
   - [7.1 合成方式の選定理由](#71-合成方式の選定理由)
   - [7.2 加算合成とアタックエンベロープ設計](#72-加算合成とアタックエンベロープ設計)
   - [7.3 ブラウザ自動再生ポリシー（Autoplay Policy）調停](#73-ブラウザ自動再生ポリシーautoplay-policy調停)
8. [レンダリングと描画パイプライン](#8-レンダリングと描画パイプライン)
   - [8.1 requestVideoFrameCallback によるカメラ同期](#81-requestvideoframecallback-によるカメラ同期)
   - [8.2 Canvas 2D ダブルパス・ネオンチューブ描画](#82-canvas-2d-ダブルパスネオンチューブ描画)
9. [楽曲シーケンスと運指誘導UX](#9-楽曲シーケンスと運指誘導ux)

---

## 1. システム概要と設計思想

`Piarno_KBL_1D` は、特殊なハードウェア（LiDAR、ToF、ウェアラブルグローブ、物理キーボード等）を排除し、**「一般的な市販端末の単眼インカメラ」＋「Web標準技術（HTML5 / Canvas / Web Audio API / WebAssembly / WebGL）」のみ**で構築されたキーボードレス・ピアノ演奏システムです。

### 設計思想の3原則
1. **Zero External Latency（低遅延の徹底）**:
   楽器演奏において20msを超える入力〜発音遅延は致命的となります。重いディープラーニング分類器や時系列LSTM、過度なスムージングをパイプラインから徹底的に排除し、幾何学的特徴量＋適応型フィルタ＋加算音響合成により、フレーム推論完了から数ミリ秒以内の発音を実現しています。
2. **Device & Distance Agnostic（距離・個体差の吸収）**:
   手の大きさやカメラとの物理的距離に左右されないよう、手の骨格自体の3Dスケールを用いた無次元正規化を行っています。
3. **Data-Driven Dynamic Calibration（データ駆動型適応）**:
   各指の骨格構造の違い（例：親指の横方向運動、薬指の独立運動の難しさ）を定数ハードコードで解決せず、実データセットから統計的に閾値を自動学習します。

---

## 2. 単眼ビジョンにおける打鍵認識の課題

単眼RGBカメラを用いた空中姿勢推定（例: MediaPipe Hands）で机面タップを検出する場合、以下の根本的な壁が存在します。

| 課題 | 現象 | 従来の失敗パターン |
| :--- | :--- | :--- |
| **深度（Z軸）の曖昧性** | 単眼カメラからのZ座標推定値はノイズが極めて大きく、ミリ単位の机面接触を区別できない。 | 画面上のZ値閾値判定では、空中での小刻みな上下運動で誤発音する。 |
| **オクルージョン（遮蔽）** | 手のひらや他の指がターゲット指を覆い隠す。 | 机面を叩いた瞬間に指先が手自体に隠れてロストする。 |
| **手ブレと遅延の二律背反** | ランドマーク座標は毎フレーム数ピクセルのジッター（震え）を含む。 | 単純な移動平均やFIRフィルタをかけると、タップ検出が数フレーム（50〜100ms）遅延する。 |
| **机面の絶対位置不定** | 机面のピクセルY座標はカメラの傾きや置き場所によって毎回変化する。 | 「CanvasのY座標 > 400 でタップ」のような絶対座標判定は設置場所が変わると破綻する。 |

本システムは、**「カメラの設置幾何（1Dアングル）」と「手の局所相対幾何」を組み合わせる**ことで、机面の絶対位置推定を行うことなく接触を確定させるアプローチを採用しました。

---

## 3. 幾何・空間正規化アルゴリズム（数理モデル）

### 3.1 机面水平ローアングル配置
カメラを机面に対して水平（スレスレ）に近いローアングルで配置します。この幾何学的配置により、指が机面に向かって押し込まれる運動が、カメラ画像上において**「垂直方向（Y軸）の明確な下向き変位」**として1次元的に強く投影されます。

### 3.2 手の基準長 $L$ による空間正規化
カメラと手との距離が変化しても同一の判定を維持するため、毎フレームの手の3D基準スケール $L$ を算出します。

手首（Landmark 0: Wrist）の3D座標を $\mathbf{P}_0 = (x_0, y_0, z_0)$、対象指の付け根関節（MCP: Metacarpophalangeal joint）の3D座標を $\mathbf{P}_{base} = (x_{base}, y_{base}, z_{base})$ とすると、手の基準長 $L$ は次式で定義されます：

$$L = \|\mathbf{P}_{base} - \mathbf{P}_0\|_2 = \sqrt{(x_{base} - x_0)^2 + (y_{base} - y_0)^2 + (z_{base} - z_0)^2}$$

※ ゼロ除算を防止するため、計算機上は $L = \max(L, 0.001)$ としてクリッピングします。

### 3.3 無次元化相対垂直変位 $r_y$ の導出
画面の高さ（ピクセル数）を $H_{canvas}$、平滑化された指先（TIP）のY座標ピクセル値を $y_{tip}$、指根元（MCPまたはPIP）のY座標ピクセル値を $y_{base}$ とします。

このとき、対象指の**無次元化相対垂直変位 $r_y$** は以下のように算出されます：

$$r_y = \frac{y_{tip} - y_{base}}{H_{canvas} \cdot L}$$

- 指先が根元より机面側（画面下方）へ強く押し込まれるほど、$r_y$ は正の大きな値をとります。
- $L$ で除算しているため、ユーザーがカメラに近づいても遠ざかっても、また手のサイズが大人と子供で異なっていても、$r_y$ の値域とダイナミクスは一定に保たれます。

---

## 4. 信号処理パイプライン（1 Euro Filter）

### 4.1 採用理由とトレードオフ
MediaPipeから出力される生のランドマーク座標には、画像量子化ノイズや推論揺らぎによる**ジッター（微小な震え）**が含まれます。
- **通常のローパスフィルタ（移動平均・指数平滑化）**: ジッターは取れるが、急峻な打鍵動作に追従できず位相遅れ（レイテンシ）が生じる。
- **カルマンフィルタ**: パラメータ調整が複雑であり、加速度の急激な変化（机面衝突）によるオーバーシュートが生じやすい。
- **1 Euro Filter（Casiez et al., 2012）**: 速度の大きさに応じてカットオフ周波数を動的に変化させる適応型1次ローパスフィルタ。静止時にはカットオフを下げて手ブレを完全除去し、高速運動時にはカットオフを跳ね上げて遅延ゼロで追従します。

### 4.2 1 Euro Filter の数理モデル

時刻 $t_i$ における入力信号を $x_i$、前回のフィルタ出力値を $\hat{x}_{i-1}$、経過時間を $dt = t_i - t_{i-1}$ とします。

#### ステップ 1: 信号の微分（速度）の平滑化
まず、生の離散速度 $dx_i = \frac{x_i - \hat{x}_{i-1}}{dt}$ を計算し、固定カットオフ周波数 $f_{c, d}$ のフィルタで平滑化します：

$$\alpha_d = \frac{1}{1 + \frac{\tau_d}{dt}}, \quad \tau_d = \frac{1}{2\pi f_{c, d}}$$

$$\hat{dx}_i = \alpha_d \cdot dx_i + (1 - \alpha_d) \cdot \hat{dx}_{i-1}$$

#### ステップ 2: 動的カットオフ周波数 $f_c$ の算出
平滑化された速度の絶対値 $|\hat{dx}_i|$ に比例して、カットオフ周波数を引き上げます：

$$f_c = f_{c, min} + \beta \cdot |\hat{dx}_i|$$

ここで、$f_{c, min}$ は最小カットオフ周波数、$\beta$ は速度比例係数です。

#### ステップ 3: 信号の適応平滑化
算出された $f_c$ を用いて平滑化係数 $\alpha$ を決定し、出力値 $\hat{x}_i$ を得ます：

$$\alpha = \frac{1}{1 + \frac{\tau}{dt}}, \quad \tau = \frac{1}{2\pi f_c}$$

$$\hat{x}_i = \alpha \cdot x_i + (1 - \alpha) \cdot \hat{x}_{i-1}$$

### 4.3 実装パラメータと動的カットオフ動作
本システム（`main.js`）では、2D関節座標用に以下のパラメータを採用しています：

```javascript
const minCutoff = 1.2;  // 静止時の最小カットオフ周波数 (Hz)
const beta = 0.008;     // 速度感度係数
const dCutoff = 1.0;    // 速度平滑化用カットオフ周波数 (Hz)
```

- **手ブレ待機時（低速）**: $f_c \approx 1.2\,\text{Hz}$ となり、微小な座標揺れが完全に遮断され、HUDや描画がピタリと安定。
- **打鍵インパクト時（高速）**: 指先が机面に振り下ろされる瞬間に $|\hat{dx}|$ が急上昇し、$f_c > 30\,\text{Hz}$ に達するため、フィルタはほぼスルー状態（Raw追従）となり遅延がゼロになります。

---

## 5. 打鍵判定ステートマシンとヒステリシス制御

### 5.1 状態遷移モデル（IDLE / TOUCHED）
打鍵判定は単一の閾値比較ではなく、**ヒステリシス（履歴現象）**を持たせた有限ステートマシン（FSM）で管理されます。

```
       r_y >= hitRyThreshold (机面押し込み)
   ┌────────────────────────────────────────┐
   │                                        ▼
┌──────┐                                ┌─────────┐
│ IDLE │                                │ TOUCHED │
└──────┘                                └─────────┘
   ▲                                        │
   └────────────────────────────────────────┘
       r_y <= liftRyThreshold (指のリフト復帰)
```

1. **`IDLE` 状態（打鍵待機）**:
   - $r_y < hitRyThreshold$ の間は発音せず待機。
   - $r_y \ge hitRyThreshold$ に達した瞬間：
     - 即座に `TOUCHED` 状態へ遷移。
     - 対象音階の音響合成を発火（ゼロレイテンシ発音）。
     - 波紋・光パーティクルエフェクトを生成。
     - 次の音符ターゲット・運指へインクリメント。
2. **`TOUCHED` 状態（接触中・リフト待機）**:
   - 指が机面に触れている間は状態を維持（多重発音を完全にガード）。
   - 指が机面から引き上げられ、$r_y \le liftRyThreshold$ を下回った瞬間に `IDLE` 状態へ復帰。

### 5.2 チャタリング遮断と高速連打（トリル）の成立
- **チャタリングの遮断**:
  机面を叩いた瞬間の反発（バウンド）や、指先の微小な振動によって $r_y$ が小刻みに上下しても、$liftRyThreshold$ を下回るまでは `TOUCHED` が維持されるため、1回の打鍵で2回音が鳴る事故（二重打鍵）が物理的に発生しません。
- **高速連打の追従**:
  ヒステリシス幅 $\Delta_{hys} = hitRyThreshold - liftRyThreshold$ を **0.03** という極小値に設定しているため、指をわずか数ミリ浮かせるだけで即座に `IDLE` に復帰し、連続打鍵やトリル演奏が可能となります。

---

## 6. データ駆動型・指別自動キャリブレーション

### 6.1 生体力学的な指ごとの可動域特性
5本の指は解剖学的に骨格配置と腱の結合が異なります：
- **親指 (THUMB)**: 鞍関節（CMC関節）構造を持ち、他の指と直交する方向へ対向運動する。変位の現れ方が独特。
- **人差し指・中指 (INDEX, MIDDLE)**: 独立した伸筋を持ち、最も可動域が広く安定して大きな $r_y$ を出力できる。
- **薬指 (RING)**: 腱が中指・小指と結合しており、単独で深く押し込むことが難しい（可動域が最も狭い）。
- **小指 (PINKY)**: 筋力が弱く、手の端にあるためカメラ画角やパースペクティブの影響を受けやすい。

全指一律の固定閾値では「薬指が鳴らない」「親指が誤爆する」問題が発生します。

### 6.2 CSVデータセット構造
本システムでは、各指の打鍵時・非打鍵時のフレームデータを記録したCSV（`public/dataset/*.csv`）を起動時に非同期ロードします。

```csv
frame,timestamp,tip_x,tip_y,base_x,base_y,L,ry,is_hit
1,16.6,640.2,350.1,638.0,280.4,0.182,0.521,0
2,33.3,640.0,365.4,638.1,280.2,0.183,0.612,1
...
```

### 6.3 統計的閾値導出アルゴリズム（インメモリ学習）
`trainModelForFinger(fingerKey)` 関数により、指ごとに以下の統計量を算出します：

1. **空中動作（Negative）の最大変位 $maxAirRy$**:
   空中浮遊動作データ（`neg_air`）の変位配列から、外れ値を除去した**第90パーセンタイル値**を空中最大限界変位として抽出。
2. **打鍵動作（Positive）の中央値 $medianHitRy$**:
   アノテーションされた打鍵フレーム（`isHit = true`）の $r_y$ の**第50パーセンタイル値（中央値）**を抽出。
3. **動的打鍵閾値 $hitRyThreshold$ の決定**:
   空中最大と打鍵中央値の内分点を閾値とします：

   $$hitRyThreshold = maxAirRy + (medianHitRy - maxAirRy) \times \gamma$$

   - 通常指（親指、人差し指、中指、小指）: $\gamma = 0.45$
   - 薬指（RING）: 可動域が狭いため $\gamma = 0.38$ に緩和（軽いタップでも確実に発音）
4. **リフト閾値 $liftRyThreshold$ の決定**:

   $$liftRyThreshold = hitRyThreshold - 0.03$$

---

## 7. ゼロレイテンシ音響合成エンジン（Web Audio API）

### 7.1 合成方式の選定理由
- **サンプリング音源（WAV/MP3）の不採用**:
  `Audio()` や `AudioBufferSourceNode` の再生では、デコード遅延、ガベージコレクション（GC）スパイク、ブラウザのバッファ制約による発音ジッター（数ms〜数十msの揺らぎ）が避けられません。
- **純粋加算合成（Additive Synthesis）の採用**:
  オシレーターノード（`OscillatorNode`）とゲインノード（`GainNode`）を打鍵の瞬間に直接 `audioCtx.currentTime` に精密スケジューリングすることで、メモリ確保ゼロ・遅延ゼロの発音を実現しています。

### 7.2 加算合成とアタックエンベロープ設計
本システムは、ピアノ弦の打撃音と共鳴特性を模した3基のオシレーター加算合成を実装しています：

```
[Osc 1: Triangle] (基本周波数 f) ───[Gain: 0.40]───┐
                                                    │
[Osc 2: Sine]     (第2倍音 2f)    ───[Gain: 0.20]───┼──►[Master Gain]──► AudioDestination
                                                    │    (Envelope)
[Osc 3: Sine]     (第3倍音 3f)    ───[Gain: 0.10]───┘
                                   (0.08s急減衰)
```

1. **基音（Triangle波 / 周波数 $f$）**:
   三角波の豊富な奇数倍音により、ピアノ本体の芯のある低〜中域の鳴りを形成。
2. **第2倍音（Sine波 / 周波数 $2f$）**:
   1オクターブ上の純音をブレンドし、ピアノ弦特有の明るさと音階の識別性を付与。
3. **第3倍音アタック（Sine波 / 周波数 $3f$）**:
   打鍵開始からわずか 80ms で指数減衰（`exponentialRampToValueAtTime(0.001, now + 0.08)`）させ、フェルトハンマーが弦を叩く「コツッ」というアタック感を再現。
4. **マスターエンベロープ**:
   - 立ち上がり（アタック）: 3ms（クリックノイズを防ぎつつ最速の立ち上がり）
   - 減衰（ディケイ〜リリース）: 260msで $0.0001$ まで指数減衰

### 7.3 ブラウザ自動再生ポリシー（Autoplay Policy）調停
近年のブラウザはユーザーインタラクションのない音声出力をブロックします。
- `ensureAudioContext()` により、初期画面タップ、カメラ起動、またはテスト発音ボタン押下時に `AudioContext.resume()` を確実に発行。
- `AudioContext` のステートが `suspended` の場合でも、打鍵直前に非同期再開を試みるセーフティガードを内蔵。

---

## 8. レンダリングと描画パイプライン

### 8.1 requestVideoFrameCallback によるカメラ同期
通常の `requestAnimationFrame` はディスプレイのリフレッシュレート（60Hz/120Hz）で発火するため、カメラの物理フレームレート（30fpsまたは60fps）との間にフレームの空振りや重複が発生します。

本システムでは、最新のブラウザAPI `video.requestVideoFrameCallback()` を優先使用：
- カメラのハードウェアISP（画像処理プロセッサ）が**新しい映像フレームをCanvasに転送したまさにその瞬間**に推論コールバックを同期発火。
- CPU/GPUの無駄な推論重複を完全に抑止し、60fps環境においてフレームジッター最小の追従を実現。

### 8.2 Canvas 2D ダブルパス・ネオンチューブ描画
ターゲット指の認識状態を演奏者に直感的に伝えるため、WebGLシェーダーを使わずに標準Canvas 2D APIで高輝度ネオン発光エフェクトを実現しています：

1. **パス 1（外側ネオングロー）**:
   `shadowBlur = 24`、`lineWidth = 6.5`、指固有のターゲットカラー（Cyan / Yellow / Green / Blue）で広域発光を描画。
2. **パス 2（内側ホワイトコア）**:
   `shadowBlur = 0`、`lineWidth = 2.6`、`strokeStyle = "rgba(255, 255, 255, 0.9)"` で中央に純白の細いコアラインを重畳。
   $\rightarrow$ 暗所・明所問わず、視覚的なコントラストとサイバーパンク調のリッチな質感を両立。

---

## 9. 楽曲シーケンスと運指誘導UX

### 9.1 シーケンス進行とターゲット指自動スイッチ
「メリーさんの羊」「聖者の行進」などの楽曲データは、ステップごとの運指番号・指キー・周波数・音名が定義されたシーケンス配列として保持されます。

```javascript
{ step: 1, phrase: 1, fingerNum: 3, fingerKey: "MIDDLE", note: "ミ", freq: 659.25 }
```

- 打鍵が成立すると、瞬時に `currentSongStep` がインクリメントされ、`setTargetFinger(nextFingerKey)` が発火。
- 追従対象の骨格、適用される学習閾値、HUD表示がシームレスに次の指へ切り替わります。

### 9.2 連続打鍵の色分けと彗星ビーム演出
演奏者が迷わないための視覚誘導アルゴリズム：
- **同一番号指の連打**: 1打目＝黄色、2打目＝緑色、3打目＝青色へと動的に発光色が変化し、打鍵リズムの把握を補助。
- **彗星ビーム（Beam Effect）**: 打鍵した瞬間の指先座標から、次に叩くべき指先座標へ向けて光のパーティクルビームを射出。視線移動を直感的にナビゲートします。

---

## まとめ

`Piarno_KBL_1D` は、単眼カメラの物理的限界を「水平アングル配置」「スケール正規化」「適応フィルタ」「ヒステリシスステートマシン」「実データ駆動キャリブレーション」の多層的なアルゴリズムで克服した、Web技術によるリアルタイム・ヒューマンインターフェースの先進事例です。
