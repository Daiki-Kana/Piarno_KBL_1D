# Piarno_KBL_1D 技術解説書 (Technical Architecture & Principles)

本ドキュメントは、単眼RGBカメラのみを用いて机面タップ（打鍵）を高精度かつ超低遅延で検出し、ブラウザ上でリアルタイムなピアノ演奏を実現するシステム `Piarno_KBL_1D` の設計思想、数理モデル、信号処理アルゴリズム、音響合成パイプラインを解説した技術仕様・解説書です。

---

## 目次

1. [システム概要と設計方針](#1-システム概要と設計方針)
2. [単眼ビジョンによる打鍵検出の課題](#2-単眼ビジョンによる打鍵検出の課題)
3. [カメラ幾何と空間正規化モデル](#3-カメラ幾何と空間正規化モデル)
   - [3.1 水平ローアングルによる1次元投影化](#31-水平ローアングルによる1次元投影化)
   - [3.2 手の3D基準長 L による距離・個体差の正規化](#32-手の3d基準長-l-による距離個体差の正規化)
   - [3.3 無次元化相対垂直変位 ry の導出](#33-無次元化相対垂直変位-ry-の導出)
4. [適応型信号平滑化（1 Euro Filter）](#4-適応型信号平滑化1-euro-filter)
   - [4.1 手ブレ除去と遅延の二律背反](#41-手ブレ除去と遅延の二律背反)
   - [4.2 1 Euro Filter の数理と動的カットオフ](#42-1-euro-filter-の数理と動的カットオフ)
5. [打鍵判定ステートマシンとヒステリシス制御](#5-打鍵判定ステートマシンとヒステリシス制御)
   - [5.1 状態遷移モデル（IDLE / TOUCHED）](#51-状態遷移モデルidle--touched)
   - [5.2 机面バウンドによる多重打鍵（チャタリング）の防止](#52-机面バウンドによる多重打鍵チャタリングの防止)
6. [データ駆動型・指別キャリブレーション](#6-データ駆動型指別キャリブレーション)
   - [6.1 生体力学的な指ごとの可動域差](#61-生体力学的な指ごとの可動域差)
   - [6.2 統計的閾値導出アルゴリズム](#62-統計的閾値導出アルゴリズム)
7. [ゼロレイテンシ音響合成エンジン（Web Audio API）](#7-ゼロレイテンシ音響合成エンジンweb-audio-api)
   - [7.1 オーディオファイル再生を排した理由](#71-オーディオファイル再生を排した理由)
   - [7.2 加算倍音合成とアタックエンベロープ](#72-加算倍音合成とアタックエンベロープ)
8. [リアルタイム描画とパイプライン統合](#8-リアルタイム描画とパイプライン統合)
   - [8.1 requestVideoFrameCallback によるカメラ同期](#81-requestvideoframecallback-によるカメラ同期)
   - [8.2 1フレーム内の処理パイプライン概要](#82-1フレーム内の処理パイプライン概要)

---

## 1. システム概要と設計方針

`Piarno_KBL_1D` は、LiDARやToFなどの深度センサー、物理キーボード、ウェアラブルデバイスを一切使わず、**「市販PC・スマートフォンの内蔵単眼カメラ」と「Web標準技術（HTML5 / Canvas / Web Audio API / WASM / WebGL）」のみ**で構築されたキーボードレス・ピアノ演奏システムです。

### 主要な設計原則
1. **極小レイテンシ（Zero External Latency）**:
   楽器演奏において、入力から発音までの遅延が20msを超えると演奏感に違和感が生じます。ディープラーニング分類器や重い時系列フィルターを排し、幾何学的特徴量抽出＋適応型ローパスフィルタ＋ブラウザ内蔵オシレーター合成により、フレーム検出から数ミリ秒以内での発音を実現します。
2. **自己正規化（Self-Normalizing）**:
   ユーザーとカメラの距離（遠近）や手の大きさ（大人と子供）による座標変化を、手の骨格自体の3D寸法を基準にして自動キャンセルします。
3. **データ駆動の閾値決定（Data-Driven Calibration）**:
   指ごとの可動域差を固定値で決め打ちせず、時系列CSVデータセットの統計量から最適閾値を自動導出します。

---

## 2. 単眼ビジョンによる打鍵検出の課題

単眼RGBカメラを用いたハンドトラッキング（MediaPipe Hands等）で「机面への接触（タップ）」を判定する場合、以下の物理的・工学的課題が存在します：

```
【上から見下ろす配置（判定困難な例）】
        [カメラ]
           │
           ▼ (深度Z軸の誤差大)
       (空中？ 机面？)
  ═══════════════════════ 机面
```

- **深度（Z軸）推定精度の低さ**:
  単眼RGB画像からの3D座標復元は曖昧性が高く、Z軸（奥行き）の推定誤差は数センチ単位に及ぶため、ミリ単位の机面接触をZ座標だけで判別することは不可能です。
- **机面の絶対位置が不定**:
  カメラの画角や傾き、設置場所によって画面内のどこが机面であるかが動的に変化します。
- **手ブレ（ジッター）と遅延のトレードオフ**:
  ランドマーク座標には微小な推論ノイズが含まれます。通常の平滑化フィルタ（移動平均等）を適用すると、急峻な打鍵動作に対して数フレーム（30〜60ms以上）の位相遅れが発生します。

本システムでは、**「カメラの設置幾何」と「手局所の相対変位モデル」を組み合わせる**ことで、机面の絶対位置推定を行わずに高精度な接触検出を可能にしました。

---

## 3. カメラ幾何と空間正規化モデル

### 3.1 水平ローアングルによる1次元投影化
カメラを机面スレスレの水平に近いローアングルに配置します。

```
【机面水平ローアングル配置】

  [カメラ] ───►  ┌──────┐ (手のひら)
                │      └─┐ (指根元: MCP)
                │        └───────► [指先: TIP] ↓ (机面への押し込み)
  ════════════════════════════════════════════════ 机面
```

この配置により、机面への打鍵動作が画面上における**「垂直方向（Y軸）の沈み込み」という明確な1次元運動**へ射影されます。深度（Z軸）の曖昧な変化を追う必要がなくなり、画面内のY軸相対運動のみで接触の物理挙動を捉えることができます。

### 3.2 手の3D基準長 L による距離・個体差の正規化
カメラとの距離や手の大きさの影響を排除するため、毎フレームの手の基準スケール `L` を算出します。

手首関節（Landmark 0: Wrist）の3D座標を `P0 = (x0, y0, z0)`、対象指の付け根関節（MCP: Metacarpophalangeal Joint）の3D座標を `P_base = (x_base, y_base, z_base)` とすると、手の基準長 `L` は両点間の3D直線距離（ユークリッド距離）として計算されます：

```text
L = √[ (x_base - x0)^2 + (y_base - y0)^2 + (z_base - z0)^2 ]
```

※ ゼロ除算を防止するため、計算上は `L = max(L, 0.001)` とします。

### 3.3 無次元化相対垂直変位 ry の導出
画面の高さを `H_canvas`（ピクセル）、平滑化された指先（TIP）のY座標を `y_tip`、指の根元関節（MCPまたはPIP）のY座標を `y_base` とします。

対象指の**無次元化相対垂直変位 `ry`** を次式で算出します：

```text
ry = (y_tip - y_base) / (H_canvas × L)
```

#### 数式の物理的意味
- **分子 `(y_tip - y_base)`**: 指先が指根元から画面下向き（机面側）にどれだけ下がっているかのピクセル差分。
- **分母 `(H_canvas × L)`**: 手がカメラに近づいて大きく映れば `L` も比例して大きくなり、離れれば小さくなるため、距離による拡大縮小率を完全に相殺します。
- **結果**: `ry` は「手のひらの長さを基準として、指先が根元から何％下へ沈み込んでいるか」を表す純粋な幾何学的比率となり、距離や手のサイズに依存しない一定の閾値比較が可能になります。

---

## 4. 適応型信号平滑化（1 Euro Filter）

### 4.1 手ブレ除去と遅延の二律背反
関節ランドマーク座標は、静止時でも毎フレーム数ピクセルの高周波ノイズ（ジッター）を含みます。
- **移動平均フィルタ / 固定ローパスフィルタ**: ノイズは抑えられるが、急峻な打鍵動作時に遅延（レイテンシ）が生じ、演奏性が損なわれる。
- **1 Euro Filter（Casiez et al., 2012）**: 信号の速度に応じてカットオフ周波数を動的に変化させる適応型1次ローパスフィルタを採用。

### 4.2 1 Euro Filter の数理と動的カットオフ

各フレーム（サンプリング周期 `dt = t_i - t_{i-1}`）において、以下の3段階で計算されます。

#### ステップ 1: 速度（信号の微分）の平滑化
前回の出力値 `x_prev` から離散速度 `dx_i` を計算し、固定カットオフ周波数 `fc_d` で平滑化します：

```text
dx_i = (x_i - x_prev) / dt

α_d = 1 / (1 + τ_d / dt)     （ただし τ_d = 1 / (2π × fc_d)）

dx_hat = α_d × dx_i + (1 - α_d) × dx_hat_prev
```

#### ステップ 2: 速度に応じた動的カットオフ周波数 fc の算出
平滑化された速度の絶対値 `|dx_hat|` に比例して、カットオフ周波数 `fc` を動的に引き上げます：

```text
fc = fc_min + β × |dx_hat|
```

ここで、`fc_min` は静止時の最小カットオフ周波数、`β` は速度感度係数です。

#### ステップ 3: 信号の適応平滑化
算出された `fc` を用いて平滑化係数 `α` を決定し、出力値 `x_hat` を得ます：

```text
α = 1 / (1 + τ / dt)         （ただし τ = 1 / (2π × fc)）

x_hat = α × x_i + (1 - α) × x_prev
```

#### 実装パラメータと動作特性
本システムでは以下のパラメータを設定しています：
- `fc_min = 1.2 Hz`（最小カットオフ周波数）
- `β = 0.008`（速度感度係数）
- `fc_d = 1.0 Hz`（速度平滑化用カットオフ周波数）

```
【動作特性】
・指が静止している時（速度小）:
  fc ≈ 1.2 Hz となり、高周波の手ブレを強力に除去（ピタリと静止）
・机面を素早く叩いた時（速度大）:
  速度項 β × |dx_hat| が支配的となり fc が数十Hzへ急上昇
  フィルタはスルー状態となり、遅延ゼロで実座標へ追従
```

---

## 5. 打鍵判定ステートマシンとヒステリシス制御

### 5.1 状態遷移モデル（IDLE / TOUCHED）
打鍵判定は、単一の閾値比較ではなく、**ヒステリシス（2値の閾値差）**を持つ有限ステートマシンで管理されます。

```
                  ① 押し込み: ry >= hitRyThreshold
                     → 即座に発音 & TOUCHED へ遷移
                ┌───────────────────────────────────┐
                │                                   ▼
          ┌──────────┐                         ┌─────────┐
          │   IDLE   │                         │ TOUCHED │
          │ (待機中) │                         │ (接触中) │
          └──────────┘                         └─────────┘
                ▲                                   │
                └───────────────────────────────────┘
                  ② リフト: ry <= liftRyThreshold
                     → IDLE へ復帰（再打鍵可能）
```

- **IDLE 状態**:
  `ry >= hitRyThreshold` に達した瞬間に打鍵と判定し、音響エンジンを発火させ、状態を `TOUCHED` に遷移させます。
- **TOUCHED 状態**:
  指先が机面に接触している間は状態を維持し、追加の発音を遮断します。
  指が机面から引き上げられ、`ry <= liftRyThreshold` を下回った瞬間に `IDLE` 状態へ復帰します。

### 5.2 机面バウンドによる多重打鍵（チャタリング）の防止
指先が机面に衝突した際、軟部組織の弾性や腱の反発により微小な物理的バウンドが発生します。単一閾値の場合、このバウンドによって閾値を複数回またぎ、1回の打鍵で連続して音が鳴る「チャタリング」が発生します。

本システムでは、打鍵閾値とリフト復帰閾値の間に以下のヒステリシス幅を設けています：

```text
liftRyThreshold = hitRyThreshold - 0.03
```

机面上で指がわずかに震えても、`0.03` の差分を上回って指を持ち上げない限りリセットされないため、多重発音を完全に防止できます。同時に、この幅は数ミリの浮上に対応するため、素早い連打（トリル）を妨げません。

---

## 6. データ駆動型・指別キャリブレーション

### 6.1 生体力学的な指ごとの可動域差
人間の手は、解剖学的に各指の可動域が大きく異なります：
- **人差し指・中指**: 独立した伸筋群を持ち、大きく安定した可動域を持つ。
- **親指**: 鞍関節（CMC関節）構造を持ち、打鍵方向と関節の屈曲軸が斜交する。
- **薬指**: 腱間結合（Junkturae Tendinum）により中指・小指と腱が連結しており、単独での可動域が極めて狭い。

全指一律の閾値を適用すると、薬指が反応しにくく、人差し指が誤爆する原因になります。

### 6.2 統計的閾値導出アルゴリズム
本システムは、事前に記録された各指の打鍵時系列CSV（`public/dataset/*.csv`）を起動時に解析し、各指の閾値を動的導出します：

1. **空中動作の上限値（maxAirRy）の算出**:
   純粋な空中動作データ（`neg_air`）の変位分布から、異常値を除外した**第90パーセンタイル値**を空中動作の最大値として定義。
2. **打鍵動作の中心値（medianHitRy）の算出**:
   打鍵アノテーションが付与されたフレーム（`isHit = true`）の変位分布から**第50パーセンタイル値（中央値）**を抽出。
3. **動的打鍵閾値（hitRyThreshold）の決定**:
   空中最大値と打鍵中央値の内分点として閾値を算出：

   ```text
   hitRyThreshold = maxAirRy + (medianHitRy - maxAirRy) × γ
   ```

   - 通常指（THUMB, INDEX, MIDDLE, PINKY）: `γ = 0.45`
   - 薬指（RING）: 可動域が狭いため `γ = 0.38`（打鍵マージンを緩和して叩きやすく調整）
4. **リフト閾値（liftRyThreshold）**:

   ```text
   liftRyThreshold = hitRyThreshold - 0.03
   ```

---

## 7. ゼロレイテンシ音響合成エンジン（Web Audio API）

### 7.1 オーディオファイル再生を排した理由
`new Audio('piano.mp3')` やサンプリング音源の再生では、ファイル読み込み、デコード遅延、ブラウザ内バッファリングにより、発音タイミングに数十ミリ秒のレイテンシとジッター（揺らぎ）が発生します。
本システムでは、外部音声ファイルを一切読み込まず、Web Audio APIの**オシレーター加算合成（Additive Synthesis）**を採用しています。

### 7.2 加算倍音合成とアタックエンベロープ
打鍵イベント発生の瞬間、`AudioContext.currentTime` を基準にして3基のオシレーターを並行駆動させ、アコースティックピアノ特有の音響特性を再現します：

```
[Osc 1: Triangle] (基本周波数 f) ───[Gain: 0.40]───┐
                                                    │
[Osc 2: Sine]     (第2倍音 2f)    ───[Gain: 0.20]───┼──►[Master Gain]──► 出力
                                                    │    (Envelope)
[Osc 3: Sine]     (第3倍音 3f)    ───[Gain: 0.10]───┘
                                   (0.08s 急減衰)
```

1. **基音（Triangle波 / 周波数 f）**:
   三角波の奇数倍音により、ピアノの木製ボディの芯のある中低域を生成。
2. **第2倍音（Sine波 / 周波数 2f）**:
   オクターブ上の純音を重ね、弦の明るい共鳴と音階の明瞭度を付与。
3. **第3倍音アタック（Sine波 / 周波数 3f）**:
   打鍵開始からわずか 80ms で指数減衰（`exponentialRampToValueAtTime(0.001, now + 0.08)`）させ、フェルトハンマーが弦を叩く瞬間の衝撃音を再現。
4. **全体エンベロープ**:
   - アタック時間: 3ms（プチッというクリックノイズを防止しつつ最速で立ち上げ）
   - ディケイ〜リリース: 260msで指数減衰

メモリ確保やファイルI/Oが発生しないため、判定フラグが立ったフレームと同一イベントループ内で遅延ゼロの発音が行われます。

---

## 8. リアルタイム描画とパイプライン統合

### 8.1 requestVideoFrameCallback によるカメラ同期
一般的な `requestAnimationFrame` は画面の描画更新（60Hz/120Hz）に同期しますが、カメラの物理フレーム到着タイミングとは一致しません。
本システムでは、最新の `HTMLVideoElement.requestVideoFrameCallback()` を優先的に使用します：
- カメラハードウェアから**新しい映像フレームがブラウザに届いた正確なタイミング**でのみ推論を実行。
- 無駄な重複推論やスキップを排除し、処理負荷を最小化しながら60fpsの安定追従を維持します。

### 8.2 1フレーム内の処理パイプライン概要

1フレーム（約16.6ms）の中で実行される処理フローは以下の通りです：

```
[カメラフレーム到着] (requestVideoFrameCallback)
       │
       ▼
[MediaPipe 推論] (GPU / WebGL デリゲートによる関節21点座標抽出)
       │
       ▼
[1 Euro Filter 平滑化] (速度に応じた適応カットオフで座標補正)
       │
       ▼
[幾何特徴量算出] (手の基準長 L の計算 → 相対垂直変位 ry の導出)
       │
       ▼
[ステートマシン判定]
  ├─► (IDLE かつ ry >= hitTh):
  │     ・Web Audio API で即時加算倍音発音（遅延ゼロ）
  │     ・波紋 & 光パーティクル描画生成
  │     ・次の運指先へビーム演出を射出
  │     ・状態を TOUCHED へ遷移
  │
  └─► (TOUCHED かつ ry <= liftTh):
        ・状態を IDLE へ復帰（リフト完了）
       │
       ▼
[Canvas 2D 描画] (ダブルパス発光ネオンチューブ & ガイドUI更新)
```

---

## まとめ

`Piarno_KBL_1D` は、単眼カメラの制約（奥行き精度の欠如、ノイズ、遅延）を、高度な深層学習に依存するのではなく、**「水平配置幾何」「手の自己正規化」「速度適応型フィルタ」「ヒステリシスステートマシン」「純粋加算合成」**という古典的かつ洗練された信号処理・幾何工学の組み合わせによって解決したアーキテクチャです。
