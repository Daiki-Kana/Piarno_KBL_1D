# マーカー膠着（フリーズ）現象の調査結果レポート

本ドキュメントは、Piarnoにおいて発生している**「空中でマーカーが固まる」**および**「画面外に手が行った後に画面内に戻すと、変な位置でマーカーが固まる」**という2つの現象について、該当コードと発生メカニズムを特定した調査結果をまとめたものです。

---

## 1. 調査概要と結論

コード解析の結果、本現象は **`main.js` 内の異常ジャンプガードにおける「基準座標更新の停止による永久膠着（デッドロック）トラップ」** が直接の原因であることが判明しました。

AIの誤検出フレームを破棄するために「前フレームの座標を維持する」処理を行っていますが、**一度でもジャンプ判定されると基準座標が更新されなくなるため、次フレーム以降も常に『古い座標と離れすぎている』と判定され続け、二度と復帰できずにその位置で永久にフリーズする構造**になっていました。

---

## 2. 根本原因の詳細解析

### 原因 ①：異常ジャンプガードの「永久膠着（デッドロック）トラップ」【最大要因】

- **該当コード**: `main.js` 1764〜1799行目
```javascript
// 1フレームでの極端な座標ジャンプの遮断ガード
let isAnomalousJump = false;
if (hasHands && lastRawLandmarks) {
  // 手首や指先の移動距離をチェック
  if (wristJumpDist > width * 0.12 || tipJumpDist > maxTipAllowedDist) {
    isAnomalousJump = true;
    landmarks = lastRawLandmarks; // ★1. 前フレームの古い座標に戻す
  }
}
if (!isAnomalousJump && hasHands) {
  lastRawLandmarks = landmarks;   // ★2. isAnomalousJumpがtrueの時は更新されない！
}
if (hasHands && !isAnomalousJump) {
  // ★3. フィルター更新もスキップされる
}
```

#### 【空中でマーカーが固まるメカニズム】
1. プレイヤーが空中で手を素早く動かしたり、指を伸ばしたりした際、1フレーム（約16ms）あたりの移動量が閾値（`maxTipAllowedDist = 画面幅の8% ≒ 102px`）を1回でも超えます。
2. ガードが作動して `isAnomalousJump = true` となり、座標が古い `lastRawLandmarks` に差し戻されます。
3. **ここで `lastRawLandmarks` が更新されずに停止します。**
4. 次のフレームで、実際の手は動いた新しい位置にいます。
5. 新しい位置の手と、古い停止したままの `lastRawLandmarks` を比較します。当然、102px以上離れたままです。
6. **次フレームも、その次のフレームも、100%確実に `isAnomalousJump = true` になります。**
7. これにより、**一度素早く動かした瞬間、マーカーがその空中の位置で永久に凍結（フリーズ）** します。

---

### 原因 ②：「画面外に手が行った後に画面内に戻すと、変な位置で固まる」メカニズム

- **該当コード**: `main.js` 1726〜1750行目、1764〜1799行目
```javascript
if (!hasHands) {
  lostFrames++;
  if (lostFrames <= MAX_LOST_FRAMES && hasValidSmoothedLandmarks) {
    // 3フレーム以内（約50ms）の一時的ロストであれば直前座標を維持
  } else {
    // 4フレーム以上で初めて lastRawLandmarks = null にリセット
    lastRawLandmarks = null;
  }
}
```

#### 【画面外復帰時に変な位置で固まるメカニズム】
1. 手が一瞬だけ画面外に出た（1〜3フレーム以内の微小な途切れ）、または画角の端から画面中央へと素早く戻ってきたとします。
2. このとき、`lastRawLandmarks` は**「画面から出ていく直前の位置（画面端など）」**を保持しています。
3. ユーザーが手を画面中央など、出て行った位置とは別の場所に戻します。
4. 画面内に戻った初フレームで、現在の手の位置と、画面端にあった直前の `lastRawLandmarks` が比較されます。
5. 当然、画面端から画面中央までの距離は画面幅の8%〜12%を遥かに超えているため、**原因①の永久膠着トラップが瞬時に発動**します。
6. その結果、**画面端（出て行く直前の位置）または戻した瞬間の中途半端な位置にマーカーが永久固定され、手自身は画面内にあるのにマーカーだけが変な位置で固まります**。

---

### 原因 ③：PointFilter の 40px クランプによる「座標引きずり（ゴム紐遅延）」

- **該当コード**: `main.js` 340〜356行目
```javascript
if (dist > this.maxStep) { // maxStep = 40.0px
  const ratio = this.maxStep / dist;
  clampedX = this.xf.xPrev + dx * ratio;
  clampedY = this.yf.xPrev + dy * ratio;
}
```

- 手が画面外からパッと戻ってきた時や、空中で素早く打鍵した際、手の移動量は1フレームで100〜200pxに達します。
- それを毎フレーム40pxずつしか進ませないため、現在の手の位置に到達するまでに数フレーム〜数十フレームの引きずり（激しい遅延）が発生し、途中で引っかかったように見えます。

---

### 原因 ④：MediaPipe 非鏡像入力時の利き手ラベル不一致

- **該当コード**: `main.js` 1705〜1725行目
- MediaPipe Hand Landmarker は生Webカメラ映像（非鏡像）を入力すると、物理的な右手を `"Left"` と判定します。
- コード側で `"right"` を探しているため、右手をかざしても「左手除外」と判定されて未検出（`hasHands = false`）になり、ロスト処理に入ってしまうケースが発生していました。

---

## 3. 恒久的な解決策（推奨改修プラン）

1. **膠着トラップ（フリーズバグ）の完全撤去**:
   - 単純に古い座標へ差し戻すだけのガードを撤去します。
   - `OneEuroFilter` は本来、静止時にはジッターを完全に抑え、高速移動時には遅延ゼロで追従する数学的アルゴリズムです。外力による安易な座標差し戻しを行わないことで、フリーズを根本解消します。
2. **PointFilterの移動量クランプ（40px制限）の撤去**:
   - 人為的な上限クランプを外し、人間の素早い手の動きに100%追従させます。
3. **MediaPipe生映像における確実な手追従**:
   - `numHands: 1` による超軽量60fpsトラッキングをベースにし、手が画面に入った瞬間に遅延なく即座に右手として捕捉させます。
4. **ロスト補間と平滑化2D基準長（`safeBaseDistPx`）の温存**:
   - 一時ロスト時のフィルター空回しと、基準手長 `L` の2D化は非常に良好に機能しているため維持します。

---

## 4. 総括

「空中で固まる」「画面外から戻すと変な位置で固まる」という症状は、**ガード処理が手の自然な高速移動や再出現を『異常なジャンプ』と誤認し、前フレーム座標への差し戻しを永久にループさせていたこと**が直接の原因です。

このガードとクランプを撤去することで、画面外からの出入りも、空中での素早い動きも、フリーズや遅延が一切なくなり、本来の滑らかなトラッキングに復旧します。
