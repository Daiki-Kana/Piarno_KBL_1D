# 左手へのマーカー飛び（座標ジャンプ・吸着）の原因解析と対策レポート

本ドキュメントは、Piarno（右手1本での机面打鍵演奏システム）において、演奏中または待機中に**「マーカー（骨格・ターゲット指）が突然左手へと飛んで吸着する（マーカー飛び）」**現象について、コード解析結果、発生メカニズム、および根本解決策をまとめたレポートです。

---

## 1. エグゼクティブサマリー

### 現象
右手で打鍵演奏を行っている最中、または手をかざして待機している際に、画角の端や机の上に置いてある**左手にマーカーが一瞬でワープ（ジャンプ）し、右手と左手を行き来して激しくブレたり、左手の位置で勝手に打鍵判定（誤発音）が発生する**。

### 根本原因
1. **`numHands: 1` による単一手の強制選択**:
   MediaPipe HandLandmarker を `numHands: 1` で動作させており、MediaPipe内部のパームディテクタが「最も検出スコアの高い1つの手」だけを返している。
2. **コード側での盲目的な採用（左右判定の欠如）**:
   `results.landmarks[0]` を無条件に「右手のランドマーク」として受け取っているため、MediaPipeが左手を選択した瞬間に、コード側は何の疑いもなくマーカーを左手に描画してしまう。
3. **打鍵動作に伴うスコア逆転**:
   右手打鍵時（指の屈伸・机面との接触・影の発生）に右手の信頼度スコアが一瞬低下した際、机の上に静止している左手のスコアが上回り、MediaPipeの出力対象が右手から左手へフレーム単位で切り替わってしまう。

---

## 2. 根本原因の詳細解析（コード該当箇所とメカニズム）

### 原因 ①：`numHands: 1` 設定と MediaPipe の手選定アルゴリズム

- **該当コード**: `main.js` 1460行目、1476行目
```javascript
handLandmarker = await HandLandmarker.createFromOptions(vision, {
  baseOptions: { modelAssetPath: modelPath, delegate: "GPU" },
  runningMode: "VIDEO",
  numHands: 1, // ★検出数を1つに限定
  minHandDetectionConfidence: 0.4,
  minHandPresenceConfidence: 0.4,
  minTrackingConfidence: 0.3
});
```

- **メカニズム**:
  1. `numHands: 1` は、フレームレート向上（60fps維持）と処理負荷軽減のために設定されています。
  2. しかし、MediaPipeは画面内に複数の手が存在する場合、**「現在追従中の手を優先する」のではなく、「そのフレームで最も手のひら検出（Palm Detection）のスコアが高い手」を1つだけ選んで `landmarks[0]` に格納**します。
  3. プレイヤーの右手は机面スレスレで動いており、指の曲げ伸ばしや机面の影、カメラの死角によって検出信頼度が細かく上下します。
  4. 一方、机に添えられている左手は静止しており、手の甲や指が綺麗に見えている場合、左手の検出スコアの方が安定して高くなる瞬間があります。
  5. その結果、MediaPipeは**前触れなく出力を右手から左手へと切り替えます**。

---

### 原因 ②：取得した手（`landmarks[0]`）の左右無検証スルー

- **該当コード**: `main.js` 1767行目
```javascript
// メインの手のランドマーク生座標（numHands: 1 で検出された手を右手演奏対象として直接追従）
const landmarks = hasHands ? results.landmarks[0] : lastRawLandmarks;
if (!landmarks) return;
lastRawLandmarks = landmarks;
```

- **メカニズム**:
  1. コード側では、`results.landmarks[0]` が右手であるか左手であるかを一切チェックしていません。
  2. そのため、MediaPipeが左手を出力した瞬間、全21関節のフィルター（`landmarkFilters`）に左手の座標がそのまま入力されます。
  3. 右手にあったマーカーは、数十〜数百ピクセル離れた左手へと一瞬でワープします。
  4. 次のフレームで右手のスコアが持ち直すと、再び右手へワープします。これが**「右手と左手を行き来する激しいマーカー飛び（フラッピング）」**の実態です。

---

### 原因 ③：なぜ以前の「左右フィルタリング（Handedness）」を撤去したのか？

過去の開発において、MediaPipeの `results.handednesses` を使って `"Right"` のみを通そうとした際、以下のような致命的不具合が発生しました（`investigation_marker_freeze.md` 参照）：

1. **Webカメラの鏡像反転とMediaPipe判定の不一致**:
   - Webカメラの生映像（自撮り視点・非反転）をそのままMediaPipeに入力すると、AIモデルは物理的な右手を **`"Left"`** と判定します。
   - CSSで画面表示を反転（`transform: scaleX(-1)`）させていても、MediaPipeに入力される生ビデオフレームは反転していないため、ラベルの逆転が発生します。
2. **打鍵時のラベル反転（Right ⇄ Left の誤認）**:
   - 机面スレスレのローアングルでは、指先を机面に向けた際に手のひらと手の甲の向きをAIが誤認し、同じ右手であっても打鍵の瞬間に `"Right"` と `"Left"` が目まぐるしく入れ替わることがあります。
3. **結果**:
   - 単純に `"Right"` のみを通すガードを入れると、物理的な右手をかざしているにもかかわらず「左手として除外（`hasHands = false`）」され、マーカーが完全に消失・凍結する現象が発生したため、前回の修正で左右フィルタリングを撤去せざるを得ませんでした。

---

## 3. マーカー飛びが引き起こす二次障害

1. **誤打鍵・勝手な発音（ゴーストタップ）**:
   マーカーが右手から左手へワープした瞬間、指先の相対変位（`relY` や急激な座標移動）が打鍵判定閾値を超え、机を叩いていないのに音が鳴ってしまう。
2. **曲進行の意図しないスキップ**:
   『よろこびのうた』などのシーケンス演奏中に左手へ飛ぶと、左手の指（例えば左手中指など）が打鍵と判定され、次の音符へ勝手に進んでしまう。
3. **視覚的な不快感と集中阻害**:
   白黒の骨格ラインやターゲットリングが左右の手の間を激しく飛び交い、プレイヤーが演奏に集中できなくなる。

---

## 4. 解決策の技術比較

| 対策案 | 手法概要 | メリット | デメリット・リスク | 評価 |
| :--- | :--- | :--- | :--- | :---: |
| **案 A<br>（位置追従ロック）** | `numHands: 2` で両手を検出し、前フレームの右手座標に最も近い手を継続追従する | MediaPipeの不安定な左右ラベルに頼らず、位置の連続性だけで100%確実に右手をロックし続けられる | 2手検出による推論負荷が僅かに増加（数ms程度） | **最推奨<br>（本命）** |
| **案 B<br>（画面領域マスク）** | 画面を左右に分割（または右手エリアを定義）し、左側領域の手は完全に無視する | 実装が極めてシンプルで確実 | プレイヤーが画面左側に寄って演奏した場合に反応しなくなる | △<br>（補助用） |
| **案 C<br>（Handedness再挑戦）** | カメラの反転設定に応じた左右ラベル（Left/Right）を厳密にマッピングする | 既存の1手検出のまま実装可能 | 机面スレスレでの角度変化による左右誤認で再び手が消えるリスクが残る | ×<br>（非推奨） |

---

## 5. 推奨恒久対策プラン（案 A の詳細実装方針）

### 方針：『位置の連続性（Nearest-Neighbor）による右手トラッキングロック』

MediaPipeのあてにならない「左右ラベル（Handedness）」に頼るのではなく、**「物理的な位置の連続性」**を利用して右手を追従し続けます。

```
[カメラ入力]
    ↓
MediaPipe HandLandmarker (numHands: 2) で最大2手検出
    ↓
【右手セレクター（selectRightHand）】
  ├─ 前フレームで追従していた右手座標がある場合:
  │    → 検出された手の中から、前回の右手位置に「最も近い手」を選択（左手を完全無視）
  │
  └─ 手が画面外から入ってきた初フレーム（未追従状態）:
       → 検出された手の中で「より右側（画面X座標が大きい方）」にある手を右手として初期ロック
    ↓
選択された右手のみを PointFilter・描画・打鍵判定へ供給
```

### 実装ステップ（変更予定箇所）

1. **MediaPipe検出数の変更 (`main.js`)**:
   `numHands: 1` → `numHands: 2` に変更し、左手が存在していても右手を見落とさないようにする。
2. **右手トラッキングロック処理の新設 (`main.js`)**:
   - `lastTrackedWristPos = { x, y }` を保持。
   - 2つの手が検出された場合、前回の `lastTrackedWristPos` とのユークリッド距離を比較し、最も近い方の手を採用。
   - 初回出現時は、自撮り鏡像配置を考慮して「画面右側（プレイヤーの右手側）」にある手を優先選択。
3. **左手の完全除外**:
   選択されなかったもう一方の手（左手）のランドマークは即座に破棄し、描画も打鍵判定も一切行わない。

---

## 6. まとめ

- 「左手へのマーカー飛び」は、**`numHands: 1` 設定によってMediaPipeが右手と左手をフレーム毎に勝手に入れ替えて出力していること**が根本原因です。
- 解決には、`numHands: 2` で両手を把握した上で、**「前フレームで追従していた右手の位置に近い方を追従し続ける（位置トラッキングロック）」**方式を採用するのが最も堅牢で確実です。

---
不明な点がある場合は必ず質問してください。
